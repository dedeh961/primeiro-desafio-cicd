apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-flask-ingress
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: api-flask
            port:
              number: 80
